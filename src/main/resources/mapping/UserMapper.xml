<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.ort//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hh.springmavendemo.dao.UserMapper">
    <resultMap id="BaseResultMap" type="com.hh.springmavendemo.model.po.User">
        <id column="id" jdbcType="INT" property="Id"/>
        <result column="username" jdbcType="VARCHAR" property="userName"/>
        <result column="password" jdbcType="VARCHAR" property="passWord"/>
    </resultMap>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from t_user
        where id = #{Id}
    </delete>

    <insert id="insert" parameterType="com.hh.springmavendemo.model.po.User">
        insert into t_user (username,password)
        values
        (#{userName},#{passWord})
    </insert>

    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select * from t_user where id = #{Id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        select * from t_user order by id asc
    </select>

</mapper>